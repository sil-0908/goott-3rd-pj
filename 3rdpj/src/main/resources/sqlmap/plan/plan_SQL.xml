<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="plan">
    <insert id="create">
    INSERT into plan(user_id, start_date, end_date, price, plan_title)
    values (#{user_id},#{start_date}, #{end_date}, #{price}, #{plan_title});
    </insert>
    <select id="list" resultType="com.goott.pj3.plan.dto.PlanDTO">
        select plan_idx, plan_title, price, user_id
        from plan
    </select>
    <select id="detail" resultType="com.goott.pj3.plan.dto.PlanDTO">
        select plan_title, plan_detail, user_id, start_date, end_date, price
        from plan
        where plan_idx = #{planIdx}
    </select>
</mapper>